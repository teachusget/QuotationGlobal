import { ArrowRight, Boxes, ChevronRight, ImageIcon, Layers3 } from 'lucide-react'
import { useEffect, useMemo, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { getMarketplaceCategories } from '../api/categories'
import { Skeleton } from '../components/ui'

const types = [{ value: 'software', label: 'Software & SaaS', description: 'Business applications and cloud platforms' }, { value: 'hardware', label: 'Hardware solutions', description: 'Devices, infrastructure and equipment' }, { value: 'services', label: 'Professional services', description: 'Expert consulting and managed services' }]

export default function BrowseSolutionsPage() {
  const navigate = useNavigate(); const [categories, setCategories] = useState([]); const [type, setType] = useState('software'); const [loading, setLoading] = useState(true); const [error, setError] = useState('')
  useEffect(() => { getMarketplaceCategories().then(setCategories).catch((requestError) => setError(requestError.message)).finally(() => setLoading(false)) }, [])
  const available = useMemo(() => categories.filter((category) => (category.service_types || []).includes(type) || (category.subcategories || []).some((subcategory) => (subcategory.service_types || []).includes(type))), [categories, type])
  const open = (category = 'All', subcategory = '') => { const parent = !subcategory && categories.find((item) => (item.subcategories || []).some((child) => child.name === category)); const resolvedCategory = parent?.name || category; const resolvedSubcategory = parent ? category : subcategory; const params = new URLSearchParams(); if (resolvedCategory !== 'All') params.set('category', resolvedCategory); if (resolvedSubcategory) params.set('subcategory', resolvedSubcategory); navigate(`/marketplace/type/${type}${params.size ? `?${params}` : ''}`) }
  return <section className="pb-10"><header className="overflow-hidden rounded-2xl bg-gradient-to-r from-[#06285e] to-primary px-6 py-9 text-white sm:px-9"><span className="inline-flex items-center gap-2 rounded-full bg-white/15 px-3 py-1.5 text-xs font-semibold"><Layers3 className="h-4 w-4"/>Solution directory</span><h1 className="mt-4 text-3xl font-bold">Browse all solutions</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-blue-100">Explore every marketplace type, category and subcategory in one place.</p></header><div className="mt-6 grid gap-5 lg:grid-cols-[280px_minmax(0,1fr)]"><aside className="h-fit rounded-2xl border bg-white p-4 shadow-subtle"><p className="px-2 text-xs font-bold uppercase tracking-wider text-slate-400">Solution type</p><div className="mt-3 space-y-2">{types.map((item) => <button type="button" key={item.value} onClick={() => setType(item.value)} className={`flex w-full items-center gap-3 rounded-xl p-3 text-left ${type === item.value ? 'bg-primary text-white shadow-sm' : 'hover:bg-blue-50'}`}><span className={`grid h-10 w-10 shrink-0 place-items-center rounded-lg ${type === item.value ? 'bg-white/15' : 'bg-slate-100 text-primary'}`}><Boxes className="h-5 w-5"/></span><span className="min-w-0 flex-1"><b className="block text-sm">{item.label}</b><span className={`mt-0.5 block text-[11px] ${type === item.value ? 'text-blue-100' : 'text-slate-500'}`}>{item.description}</span></span><ChevronRight className="h-4 w-4"/></button>)}</div><button type="button" onClick={() => open('All')} className="mt-4 flex h-11 w-full items-center justify-center gap-2 rounded-xl border border-primary text-xs font-bold text-primary hover:bg-blue-50">View all solutions<ArrowRight className="h-4 w-4"/></button></aside><main><div className="mb-4 flex items-end justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wider text-primary">Categories</p><h2 className="mt-1 text-2xl font-bold">{types.find((item) => item.value === type)?.label}</h2></div><span className="text-xs font-semibold text-slate-500">{available.length} categories</span></div>{error && <p className="rounded-xl bg-red-50 p-4 text-sm text-red-700">{error}</p>}<div className="grid gap-4 md:grid-cols-2">{loading && Array.from({ length: 6 }, (_, index) => <Skeleton key={index} className="h-56"/>)}{available.map((category) => { const subs = (category.subcategories || []).filter((item) => (item.service_types || []).includes(type)); return <article key={category.id} className="rounded-2xl border bg-white p-5 shadow-subtle transition hover:border-blue-200 hover:shadow-floating"><button type="button" onClick={() => open(category.name)} className="flex w-full items-center gap-3 text-left"><span className="grid h-12 w-12 shrink-0 place-items-center overflow-hidden rounded-xl bg-slate-50">{category.logo_url ? <img src={category.logo_url} alt="" className="h-full w-full object-contain"/> : <ImageIcon className="h-5 w-5 text-slate-400"/>}</span><span className="min-w-0 flex-1"><b className="block text-base text-slate-900">{category.name}</b><span className="mt-1 block text-xs text-slate-500">{subs.length} subcategories</span></span><ChevronRight className="h-5 w-5 text-slate-400"/></button><div className="mt-4 flex flex-wrap gap-2 border-t pt-4">{subs.map((subcategory) => <button type="button" key={subcategory.id} onClick={() => open(subcategory.name)} className="rounded-full bg-slate-100 px-3 py-2 text-xs font-medium text-slate-600 hover:bg-blue-50 hover:text-primary">{subcategory.name}</button>)}{!subs.length && <span className="text-xs text-slate-400">Explore all {category.name} solutions</span>}</div></article>})}{!loading && !available.length && <div className="col-span-full rounded-2xl border border-dashed bg-white p-12 text-center text-sm text-slate-500">No categories are available for this solution type.</div>}</div></main></div></section>
}
